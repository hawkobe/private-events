# README

This project is designed to mimic (structurally) a site like Eventbrite that allows users to create events, attend events, and have events that can be attended by many users.

It is my first attempt to make a database structure that has many to many associations using through tables and using customized name associations, instead of ruby default provided ones.
